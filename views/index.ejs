<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <title>HOME UPDATE</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <nav class="nav">
    <div class="brand">
      <img src="/logo.png" alt="HOME UPDATE" class="nav-logo" />
      <span class="brand-text">HOME UPDATE</span>
    </div>
    <div class="actions">
      <% if (user) { %>
        <a class="btn" href="/dashboard">DASHBOARD</a>
        <a class="btn danger" href="/logout">DÉCONNEXION</a>
      <% } else { %>
        <a class="btn primary" href="/login">CONNEXION DISCORD</a>
      <% } %>
    </div>
  </nav>

  <main class="container">
    <header class="hero" style="text-align:left;">
      <h1>HOME UPDATE</h1>
      <p>SYSTÈME AUTOMATISÉ DE MISE À JOUR POUR TON BOT DISCORD.</p>
      <span class="version-pill">DERNIÈRE VERSION : <%= version %></span>
    </header>

    <section class="release-strip">
      <div class="strip-left">
        <div class="version-badge"><%= version %></div>
        <div class="strip-info">
          <h3>DERNIÈRE RELEASE</h3>
          <% if (last) { %>
            <p class="notes"><%= last.notes || "Aucune note fournie." %></p>
          <% } else { %>
            <p class="notes">Aucune release disponible.</p>
          <% } %>
        </div>
      </div>
      <div class="strip-right">
        <div class="metric">
          <div class="metric-title">DATE</div>
          <div class="metric-value"><%= date %></div>
        </div>
        <div class="metric">
          <div class="metric-title">TÉLÉCHARGEMENTS</div>
          <div class="metric-value"><%= downloads %></div>
        </div>
      </div>
    </section>

    <section class="home-tiles">
      <div class="tile">
        <h2>COMMENT METTRE À JOUR TON BOT</h2>
        <ul class="steps">
          <li>1. Sur Discord, envoie la commande <strong>+UPDATE</strong>.</li>
          <li>2. Le bot contacte HOME UPDATE et télécharge la dernière version.</li>
          <li>3. Les fichiers sont mis à jour et le bot redémarre automatiquement.</li>
        </ul>
        <p class="muted">
          Aucun accès FTP, aucune intervention manuelle : tout passe par la commande et l’API.
        </p>
      </div>

      <div class="tile">
        <h2>STATISTIQUES</h2>
        <div class="stat-grid">
          <div class="stat">
            <div class="stat-kpi"><%= totalBots %></div>
            <div class="stat-label">BOTS ENREGISTRÉS</div>
          </div>
          <div class="stat">
            <div class="stat-kpi"><%= upToDate %></div>
            <div class="stat-label">BOTS À JOUR</div>
          </div>
          <div class="stat">
            <div class="stat-kpi"><%= outdated %></div>
            <div class="stat-label">BOTS OBSOLÈTES</div>
          </div>
        </div>
      </div>
    </section>
  </main>

  <footer class="footer">
    © <%= new Date().getFullYear() %> — HOME UPDATE PANEL ·
    <a href="<%= support %>" target="_blank" rel="noopener">DISCORD SUPPORT</a>
  </footer>
</body>
</html>
