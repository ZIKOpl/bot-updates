<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Bot Updates</title>
  <link rel="stylesheet" href="/style.css" />
  <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.6/dist/chart.umd.min.js" defer></script>
</head>
<body>
  <nav class="nav">
    <div class="brand">
      <div class="logo"></div>
      <h1>Bot Updates</h1>
    </div>
    <div class="actions">
      <a href="/dashboard" class="btn primary">Dashboard</a>
    </div>
  </nav>

  <main class="container">
    <section class="hero">
      <h1>📦 Système de mise à jour automatique</h1>
      <p>Gérez et suivez les versions de vos bots facilement.</p>
      <span class="version-pill">Version actuelle : <%= version %></span>
    </section>

    <div class="grid">
      <div class="card">
        <h2>📊 Statistiques des bots</h2>
        <canvas id="versionChart"></canvas>
      </div>

      <div class="card">
        <h2>🚀 Dernière mise à jour</h2>
        <p><strong>Version :</strong> <%= version %></p>
        <p><strong>Date :</strong> <%= date %></p>
        <p><strong>Nombre de téléchargements :</strong> <%= downloads || 0 %></p>
      </div>
    </div>
  </main>

  <footer class="footer">
    <p>© 2025 Bot Updates — Propulsé par Render & Node.js</p>
  </footer>

  <script defer>
    document.addEventListener("DOMContentLoaded", () => {
      // ✅ Correction : parser les JSON générés par EJS
      const upToDate = JSON.parse('<%- JSON.stringify(upToDate || 0) %>');
      const outdated = JSON.parse('<%- JSON.stringify(outdated || 0) %>');

      const ctx = document.getElementById("versionChart");
      if (!ctx) return;

      new Chart(ctx, {
        type: "doughnut",
        data: {
          labels: ["À jour", "Anciennes versions"],
          datasets: [{
            data: [upToDate, outdated],
            backgroundColor: ["#4caf50", "#ff5252"],
            borderWidth: 1.5
          }]
        },
        options: {
          plugins: {
            legend: {
              labels: { color: "#ddd", font: { size: 14 } }
            }
          }
        }
      });
    });
  </script>
</body>
</html>
