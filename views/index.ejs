<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>🤖 Bot Updates</title>
  <style>
    body {
      background-color: #0f1116;
      color: #fff;
      font-family: "Inter", sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }
    .container {
      background: #1a1d23;
      padding: 40px;
      border-radius: 15px;
      box-shadow: 0 0 20px rgba(0,0,0,0.3);
      text-align: center;
      width: 90%;
      max-width: 450px;
      animation: fadeIn 0.4s ease-in-out;
    }
    h1 { color: #7289da; margin-bottom: 10px; }
    p { color: #ccc; }
    button {
      background: #5865f2;
      color: white;
      border: none;
      border-radius: 8px;
      padding: 10px 20px;
      font-size: 16px;
      margin: 10px;
      cursor: pointer;
      transition: 0.2s;
    }
    button:hover { background: #4752c4; }
    .version { margin: 10px 0; font-weight: 600; }
    a { text-decoration: none; }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🤖 Bot Updates</h1>
    <p class="version">Version actuelle : <strong><%= version %></strong></p>

    <% if (user) { %>
      <p>Connecté en tant que <strong><%= user.username %></strong></p>
      <a href="/dashboard"><button>Accéder au dashboard</button></a><br>
      <a href="/logout"><button style="background:#e74c3c;">Se déconnecter</button></a>
    <% } else { %>
      <a href="/login"><button>Connexion via Discord</button></a>
    <% } %>
  </div>
</body>
</html>
