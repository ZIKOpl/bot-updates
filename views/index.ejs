<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <title>Bot Updates</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <!-- NAV -->
  <nav class="nav">
    <div class="brand">
      <img src="/logo.png" alt="HOME UPDATE" class="nav-logo" />
      <span class="brand-text">HOME UPDATE</span>
    </div>
    <div class="actions">
      <% if (user) { %>
        <a class="btn" href="/dashboard">Dashboard</a>
        <a class="btn danger" href="/logout">Déconnexion</a>
      <% } else { %>
        <a class="btn primary" href="/login">Connexion Discord</a>
      <% } %>
    </div>
  </nav>

  <main class="container">
    <header class="hero">
      <h1>Gestionnaire de mise à jour</h1>
      <p>Automatise les updates de ton bot Discord facilement.</p>
      <span class="version-pill">Dernière version : <%= version %></span>
    </header>

    <!-- STRIP dernière release -->
    <section class="release-strip">
      <div class="strip-left">
        <div class="version-badge"><%= version %></div>
        <div class="strip-info">
          <h3>Dernière release</h3>
          <% if (last) { %>
            <p class="notes"><%= (last.notes && last.notes.length) ? last.notes : "Aucune note fournie pour cette version." %></p>
          <% } else { %>
            <p class="notes">Aucune release disponible.</p>
          <% } %>
        </div>
      </div>
      <div class="strip-right">
        <div class="metric">
          <div class="metric-title">Date</div>
          <div class="metric-value"><%= date %></div>
        </div>
        <div class="metric">
          <div class="metric-title">Téléchargements</div>
          <div class="metric-value"><%= downloads || 0 %></div>
        </div>
      </div>
    </section>

    <!-- Deux tuiles centrées et espacées -->
    <section class="home-tiles">
      <div class="tile">
        <h2>Statistiques</h2>
        <p>Total de bots : <strong><%= totalBots %></strong></p>
        <p>À jour : <strong><%= upToDate %></strong></p>
        <p>Obsolètes : <strong><%= outdated %></strong></p>
      </div>

      <div class="tile">
        <h2>Accès</h2>
        <% if (user) { %>
          <p>Connecté en tant que : <strong><%= user.username %></strong></p>
          <a href="/dashboard" class="btn primary">Aller au Dashboard</a>
        <% } else { %>
          <p>Connecte-toi pour gérer les versions.</p>
          <a href="/login" class="btn primary">Connexion Discord</a>
        <% } %>
      </div>
    </section>
  </main>

  <footer class="footer">© <%= new Date().getFullYear() %> — Bot Updates Panel</footer>
</body>
</html>
