<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <title>HOME UPDATE • Accueil</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <!-- NAV -->
  <nav class="nav">
    <div class="brand">
      <img src="/logo.png" alt="HOME UPDATE" class="nav-logo" />
      <span class="brand-text">HOME UPDATE</span>
    </div>
    <div class="nav-right">
      <% if (user) { %>
        <a class="btn" href="/dashboard">Dashboard</a>
        <a class="btn danger" href="/logout">Déconnexion</a>
      <% } else { %>
        <a class="btn primary" href="/login">Connexion Discord</a>
      <% } %>
    </div>
  </nav>

  <main class="container">
    <header class="hero">
      <h1>Mises à jour de ton bot, sans prise de tête</h1>
      <p>Dépose ta release, appelle <strong>+update</strong> sur le bot, il télécharge, installe et redémarre tout seul.</p>
      <span class="version-pill">Dernière version : <%= version %></span>
    </header>

    <!-- Bandeau dernière release -->
    <section class="release-strip">
      <div class="strip-left">
        <div class="version-badge"><%= version %></div>
        <div class="strip-info">
          <h3>Dernière release</h3>
          <% if (last) { %>
            <p class="notes"><%= (last.notes && last.notes.length) ? last.notes : "Aucune note fournie pour cette version." %></p>
          <% } else { %>
            <p class="notes">Aucune release disponible.</p>
          <% } %>
        </div>
      </div>
      <div class="strip-right">
        <div class="metric">
          <div class="metric-title">Date</div>
          <div class="metric-value"><%= date %></div>
        </div>
        <div class="metric">
          <div class="metric-title">Téléchargements</div>
          <div class="metric-value"><%= downloads || 0 %></div>
        </div>
      </div>
    </section>

    <!-- Tuiles -->
    <section class="home-tiles">
      <div class="tile">
        <h2>📘 Comment mettre à jour ton bot</h2>
        <p>1) Sur Discord, tape <code>+update</code>.</p>
        <p>2) Le bot télécharge la dernière release et installe les fichiers.</p>
        <p>3) Le bot redémarre automatiquement.</p>
        <p class="muted" style="margin-top:8px">En cas de souci, vérifie le token ou re-déploie la release.</p>
      </div>

      <div class="tile">
        <h2>📈 Statistiques</h2>
        <p>Total de bots : <strong><%= totalBots %></strong></p>
        <p>À jour : <strong><%= upToDate %></strong></p>
        <p>Obsolètes : <strong><%= outdated %></strong></p>
      </div>
    </section>
  </main>

  <footer class="footer">© <%= new Date().getFullYear() %> — Home Update Panel</footer>
</body>
</html>
