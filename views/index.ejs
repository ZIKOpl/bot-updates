<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <title>Bot Updates</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet" href="/style.css" />
  <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.6/dist/chart.umd.min.js" defer></script>
  <script defer>
    document.addEventListener("DOMContentLoaded", () => {
      const upToDate = <%= JSON.stringify(upToDate) %>;
      const outdated = <%= JSON.stringify(outdated) %>;
      const ctx = document.getElementById("versionChart");
      if (!ctx) return;

      new Chart(ctx, {
        type: "doughnut",
        data: {
          labels: ["À jour", "Anciennes versions"],
          datasets: [{
            data: [upToDate, outdated],
            backgroundColor: ["#4caf50", "#ff5252"]
          }]
        },
        options: {
          plugins: { legend: { labels: { color: "#ddd" } } }
        }
      });
    });
  </script>
</head>

<body>
  <header class="nav">
    <div class="brand"><div class="logo"></div><span>Bot Updates</span></div>
    <nav class="actions">
      <% if (user) { %>
        <a class="btn ghost" href="/dashboard">Dashboard</a>
        <a class="btn danger" href="/logout">Déconnexion</a>
      <% } else { %>
        <a class="btn primary" href="/login">Connexion</a>
      <% } %>
    </nav>
  </header>

  <main class="container">
    <section class="hero">
      <h1>Gestion des mises à jour</h1>
      <p>Suivi automatique des versions de votre bot Discord.</p>
      <div class="version-pill">Dernière version : <strong><%= version %></strong></div>
    </section>

    <section class="grid">
      <div class="card">
        <h2>📊 Statistiques</h2>
        <p>Total de bots : <strong><%= totalBots %></strong></p>
        <p>Téléchargements : <strong><%= downloads %></strong></p>
        <canvas id="versionChart" height="160"></canvas>
      </div>

      <div class="card">
        <h2>📦 Versions disponibles</h2>
        <% if (releases.length) { %>
          <div class="release-list">
            <% releases.forEach(r => { %>
              <div class="release-item">
                <div class="v">Version <%= r.version %></div>
                <div class="meta"><%= (r.size / 1024 / 1024).toFixed(2) %> Mo</div>
              </div>
            <% }) %>
          </div>
        <% } else { %>
          <p class="empty">Aucune version publiée.</p>
        <% } %>
      </div>
    </section>
  </main>

  <footer class="footer">
    <span>© <script>document.write(new Date().getFullYear())</script> Bot Updates</span>
  </footer>
</body>
</html>
