<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <title>HOME UPDATE • Accueil</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <!-- NAV -->
  <nav class="nav">
    <div class="brand">
      <img src="/logo.png" alt="HOME UPDATE" class="nav-logo" />
      <span class="brand-text">HOME UPDATE</span>
    </div>
    <div class="nav-right">
      <a class="btn" href="/support">Discord</a>
      <% if (user) { %>
        <a class="btn" href="/dashboard">Dashboard</a>
        <a class="btn danger" href="/logout">Déconnexion</a>
      <% } else { %>
        <a class="btn primary" href="/login">Connexion Discord</a>
      <% } %>
      <div class="logo-right">
        <img src="/logo.png" alt="" />
        <span>HOME UPDATE</span>
      </div>
    </div>
  </nav>

  <main class="container">
    <!-- HERO -->
    <header class="hero">
      <h1>Mises à jour de ton bot, sans prise de tête</h1>
      <p>Glisse-dépose la nouvelle version sur le dashboard. Les bots se mettent à jour avec <code>+update</code> et redémarrent automatiquement.</p>
      <span class="version-pill">Dernière version : <%= version %></span>
    </header>

    <!-- Dernière release (strip) -->
    <section class="release-strip card-glow">
      <div class="strip-left">
        <div class="version-badge"><%= version %></div>
        <div class="strip-info">
          <h3>Dernière release</h3>
          <% if (last) { %>
            <p class="notes"><%= (last.notes && last.notes.length) ? last.notes : "Aucune note fournie pour cette version." %></p>
          <% } else { %>
            <p class="notes">Aucune release disponible.</p>
          <% } %>
        </div>
      </div>
      <div class="strip-right">
        <div class="metric">
          <div class="metric-title">Date</div>
          <div class="metric-value"><%= date %></div>
        </div>
        <div class="metric">
          <div class="metric-title">Téléchargements</div>
          <div class="metric-value"><%= downloads || 0 %></div>
        </div>
      </div>
    </section>

    <!-- Tutoriel -->
    <section class="home-tiles">
      <div class="tile card-tilt">
        <h2>Comment mettre à jour ton bot</h2>
        <ol class="steps">
          <li>Sur Discord, tape <code>+update</code> dans un salon où ton bot a accès.</li>
          <li>Le bot télécharge la dernière version et installe les fichiers.</li>
          <li>Le bot redémarre automatiquement. C’est tout ✅</li>
        </ol>
        <div class="cta-row">
          <a class="btn primary" href="/support">💬 Rejoindre le Discord</a>
          <a class="btn" href="/terms">📄 Termes & Conditions</a>
        </div>
      </div>

      <div class="tile card-tilt">
        <h2>Statistiques globales</h2>
        <p class="muted">Aperçu rapide de l’écosystème.</p>
        <div class="stat-grid">
          <div class="stat">
            <div class="stat-kpi"><%= totalBots %></div>
            <div class="stat-label">Bots vus</div>
          </div>
          <div class="stat">
            <div class="stat-kpi"><%= upToDate %></div>
            <div class="stat-label">À jour</div>
          </div>
          <div class="stat">
            <div class="stat-kpi"><%= outdated %></div>
            <div class="stat-label">Obsolètes</div>
          </div>
        </div>
        <% if (!user) { %>
          <div class="cta-row" style="margin-top:12px">
            <a class="btn primary" href="/login">Se connecter au dashboard</a>
          </div>
        <% } %>
      </div>
    </section>
  </main>

  <footer class="footer">
    © <%= new Date().getFullYear() %> — HOME UPDATE Panel ·
    <a href="/terms">Termes</a> ·
    <a href="/support">Support</a> ·
    <a href="<%= support %>" target="_blank" rel="noopener">Discord</a>
  </footer>
</body>
</html>
