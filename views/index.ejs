<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <title>HOME UPDATE</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <nav class="nav">
    <div class="brand">
      <img src="/logo.png" alt="HOME UPDATE" class="nav-logo" />
      <span class="brand-text">HOME UPDATE</span>
    </div>

    <button class="menu-toggle" id="menuToggle" aria-label="Menu">☰</button>

    <div class="nav-right" id="navMenu">
      <% if (user) { %>
        <a class="btn" href="/dashboard">Dashboard</a>
        <a class="btn danger" href="/logout">Déconnexion</a>
      <% } else { %>
        <a class="btn primary" href="/login">Connexion Discord</a>
      <% } %>
    </div>
  </nav>

  <main class="container">
    <header class="hero" style="text-align:left;">
      <h1>HOME UPDATE</h1>
      <p>Système automatisé de mise à jour — simple, rapide, efficace.</p>
      <span class="version-pill">Dernière version : <%= version %></span>
    </header>

    <section class="release-strip">
      <div class="strip-left">
        <div class="version-badge"><%= version %></div>
        <div class="strip-info">
          <h3>Dernière release</h3>
          <% if (last) { %>
            <p class="notes"><%= last.notes || "Aucune note fournie." %></p>
          <% } else { %>
            <p class="notes">Aucune release disponible.</p>
          <% } %>
        </div>
      </div>
      <div class="strip-right">
        <div class="metric"><div class="metric-title">Date</div><div class="metric-value"><%= date %></div></div>
        <div class="metric"><div class="metric-title">Téléchargements</div><div class="metric-value"><%= downloads %></div></div>
      </div>
    </section>

    <section class="home-tiles">
      <div class="tile card-tilt">
        <h2>COMMENT METTRE À JOUR TON BOT</h2>
        <ul class="steps">
          <li>1. SUR DISCORD, ENTRE <strong>+UPDATE</strong>.</li>
          <li>2. ATTENDS QUE LE BOT INSTALLE LES FICHIERS.</li>
          <li>3. LE BOT SE RELANCE AUTOMATIQUEMENT.</li>
        </ul>
      </div>

      <div class="tile card-tilt">
        <h2>STATISTIQUES</h2>
        <div class="stat-grid">
          <div class="stat"><div class="stat-kpi"><%= totalBots %></div><div class="stat-label">Total bots</div></div>
          <div class="stat"><div class="stat-kpi"><%= upToDate %></div><div class="stat-label">À jour</div></div>
          <div class="stat"><div class="stat-kpi"><%= outdated %></div><div class="stat-label">Obsolètes</div></div>
        </div>
      </div>
    </section>
  </main>

  <footer class="footer">
    © <%= new Date().getFullYear() %> — HOME UPDATE Panel ·
    <a href="<%= support %>" target="_blank" rel="noopener">Discord</a>
  </footer>

  <script>
    const toggle = document.getElementById("menuToggle");
    const menu = document.getElementById("navMenu");
    toggle.addEventListener("click", () => menu.classList.toggle("show"));
    window.addEventListener("click", e => {
      if (!toggle.contains(e.target) && !menu.contains(e.target))
        menu.classList.remove("show");
    });
  </script>
</body>
</html>
