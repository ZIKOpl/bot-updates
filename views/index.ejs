<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Mises à jour du bot</title>
  <style>
    body {
      font-family: "Poppins", sans-serif;
      background: #0e0e0e;
      color: #fff;
      text-align: center;
      padding: 50px;
    }
    .card {
      background: #161616;
      border-radius: 15px;
      padding: 30px;
      margin: auto;
      width: 90%;
      max-width: 600px;
      box-shadow: 0 0 15px rgba(255, 90, 0, 0.2);
    }
    h1 { color: #ff3b3b; }
    h2 { color: #ffaa33; }
    .stats {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 15px;
      margin-top: 20px;
    }
    .stat {
      background: #1c1c1c;
      padding: 15px;
      border-radius: 10px;
    }
    .stat h3 { color: #00ffaa; margin: 5px; }
    .btn {
      display: inline-block;
      margin-top: 20px;
      color: #ffaa33;
      text-decoration: none;
      border: 1px solid #ffaa33;
      border-radius: 8px;
      padding: 10px 20px;
      transition: 0.3s;
    }
    .btn:hover { background: #ffaa33; color: #000; }
    .logout {
      background: #ff3b3b;
      border: 1px solid #ff3b3b;
      margin-left: 10px;
    }
    .logout:hover {
      background: #ff5c5c;
      color: #000;
    }
  </style>
</head>
<body>
  <div class="card">
    <h1>🚀 Système de mise à jour</h1>
    <h2>Version actuelle : <%= version %></h2>

    <div class="stats">
      <div class="stat">
        <h3><%= totalBots %></h3>
        <p>Bots enregistrés</p>
      </div>
      <div class="stat">
        <h3><%= upToDate %></h3>
        <p>À jour</p>
      </div>
      <div class="stat">
        <h3><%= outdated %></h3>
        <p>Non à jour</p>
      </div>
      <div class="stat">
        <h3><%= downloads %></h3>
        <p>Téléchargements</p>
      </div>
    </div>
  </div>

  <% if (!user) { %>
    <a href="/login" class="btn">Connexion Discord (Admin)</a>
  <% } else { %>
    <a href="/dashboard" class="btn">Aller au tableau de bord</a>
    <a href="/logout" class="btn logout">Se déconnecter</a>
  <% } %>

  <p style="margin-top:40px; color:#888;">© <%= new Date().getFullYear() %> • Système de mise à jour by ZIKO</p>
</body>
</html>
