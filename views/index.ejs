<div class="box">
  <h1>🤖 Bot Updates</h1>
  <p>Version actuelle : <strong><%= version %></strong></p>

  <% if (user) { %>
    <p>Connecté en tant que <strong><%= user.username %></strong></p>
    <a href="/dashboard"><button>Aller au dashboard</button></a>
    <a href="/logout"><button>Se déconnecter</button></a>
  <% } else { %>
    <a href="/login"><button>Connexion Discord</button></a>
  <% } %>
</div>
