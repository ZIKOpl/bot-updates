<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bot Updates Panel</title>
  <link rel="stylesheet" href="/style.css?v=4">
</head>
<body>
  <nav class="nav">
    <div class="brand">
      <div class="logo"></div>
      <span>Bot Updates</span>
    </div>
    <div class="actions">
      <% if (user) { %>
        <a href="/dashboard" class="btn primary">Dashboard</a>
        <a href="/logout" class="btn danger">Déconnexion</a>
      <% } else { %>
        <a href="/login" class="btn primary">Connexion Discord</a>
      <% } %>
    </div>
  </nav>

  <header class="hero fade-in">
    <h1>🚀 Gestionnaire de mise à jour</h1>
    <p>Gère et distribue automatiquement les versions de ton bot Discord en toute simplicité.</p>
    <span class="version-pill">Dernière version : <%= version %></span>
  </header>

  <main class="container home-layout fade-in">
    <section class="card home-card">
      <h2>📦 Dernière Release</h2>
      <p><strong>Version :</strong> <%= version %></p>
      <p><strong>Date :</strong> <%= date %></p>
      <p><strong>Téléchargements :</strong> <%= downloads %></p>
    </section>

    <section class="card home-card">
      <h2>📊 Statistiques</h2>
      <p>Total de bots : <%= totalBots %></p>
      <p>À jour : <%= upToDate %></p>
      <p>Obsolètes : <%= outdated %></p>
    </section>

    <section class="card home-card">
      <h2>⚙️ Informations</h2>
      <p>Serveur actif : ✅</p>
      <p>Dernière mise à jour : <%= new Date().toLocaleString("fr-FR") %></p>
      <p>Panel : en ligne</p>
    </section>
  </main>

  <footer class="footer">
    <p>© 2025 — Bot Updates Panel</p>
  </footer>
</body>
</html>
