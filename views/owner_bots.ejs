<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <title>Bots Clients • HOME UPDATE</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet" href="/style.css" />
  <style>
    .bot-row { cursor:pointer; transition:background .2s; padding:10px; border-bottom:1px solid #333; }
    .bot-row:hover { background:rgba(255,255,255,0.05); }
    .bot-details { display:none; background:#141414; padding:10px 15px; border-left:3px solid #6c8cff; margin-bottom:8px; border-radius:8px; }
    .bot-details.open { display:block; }
    .token-mask { background:#222; padding:3px 8px; border-radius:5px; font-family:monospace; color:#ccc; }
  </style>
</head>
<body>
  <nav class="nav">
    <div class="brand">
      <img src="/logo.png" alt="HOME UPDATE" class="nav-logo" />
      <span class="brand-text">HOME UPDATE</span>
    </div>
    <div class="nav-right">
      <a class="btn" href="/dashboard">Dashboard</a>
      <a class="btn danger" href="/logout">Déconnexion</a>
    </div>
  </nav>

  <main class="container">
    <header class="hero">
      <h1>Gestion des Bots</h1>
      <p>Liste de tous les bots enregistrés dans la base.</p>
    </header>

    <div class="card">
      <% if (bots.length === 0) { %>
        <p class="muted">Aucun bot enregistré pour le moment.</p>
      <% } else { %>
        <% bots.forEach(bot => { %>
          <div class="bot-row" data-id="<%= bot._id %>">
            <strong><%= bot.name %></strong> — ajouté le <%= new Date(bot.createdAt).toLocaleString("fr-FR",{dateStyle:"medium", timeStyle:"short"}) %>
            <div class="bot-details" id="details-<%= bot._id %>">
              <p><b>ID du bot :</b> <%= bot._id %></p>
              <p><b>Propriétaire :</b> <%= bot.ownerId %></p>
              <p><b>Créé le :</b> <%= new Date(bot.createdAt).toLocaleString("fr-FR",{dateStyle:"medium", timeStyle:"short"}) %></p>
              <p><b>Token :</b> <span class="token-mask">•••••••••••••••••••••••••••••••</span></p>
              <% if (bot.meta && bot.meta.notes) { %>
                <p><b>Notes :</b> <%= bot.meta.notes %></p>
              <% } %>
              <% if (bot.stats) { %>
                <p><b>Dernier Ready :</b> <%= bot.stats.lastReady ? new Date(bot.stats.lastReady).toLocaleString("fr-FR",{dateStyle:"medium", timeStyle:"short"}) : "–" %></p>
                <p><b>Redémarrages :</b> <%= bot.stats.restarts || 0 %> · <b>Erreurs :</b> <%= bot.stats.errors || 0 %></p>
              <% } %>
            </div>
          </div>
        <% }) %>
      <% } %>
    </div>
  </main>

  <footer class="footer">
    © <%= new Date().getFullYear() %> — HOME UPDATE Panel ·
    <a href="https://discord.gg/b9tS35tkjN" target="_blank" rel="noopener">Discord</a>
  </footer>

  <script>
    document.querySelectorAll('.bot-row').forEach(row => {
      row.addEventListener('click', () => {
        const id = row.dataset.id;
        document.getElementById('details-' + id).classList.toggle('open');
      });
    });
  </script>
</body>
</html>
