<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Accès refusé</title>
  <link rel="preconnect" href="https://fonts.googleapis.com"/>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet"/>
  <style>
    :root{--bg:#0b0f16;--card:#121826;--text:#e5e7eb;--muted:#9aa4b2;--brand:#7c5cff}
    *{box-sizing:border-box}
    body{margin:0;height:100vh;display:grid;place-items:center;font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;background:radial-gradient(80% 80% at 50% 0%, rgba(124,92,255,.15), transparent 60%),var(--bg);color:var(--text)}
    .card{width:min(560px,94vw);background:linear-gradient(180deg,rgba(255,255,255,.02),rgba(255,255,255,.01));border:1px solid #1f2937;border-radius:16px;padding:22px;text-align:center;box-shadow:0 6px 18px rgba(0,0,0,.25)}
    h1{margin:0 0 6px}
    p{color:var(--muted)}
    .btn{display:inline-block;margin-top:14px;padding:10px 14px;border-radius:12px;background:linear-gradient(135deg,var(--brand),#4f46e5);color:#fff;text-decoration:none;font-weight:700}
  </style>
</head>
<body>
  <div class="card">
    <h1>🔒 Accès refusé</h1>
    <p>Ce dashboard est réservé au propriétaire du service.</p>
    <% if (!user) { %>
      <a class="btn" href="/login">Se connecter avec Discord</a>
    <% } else { %>
      <a class="btn" href="/">Retour à l’accueil</a>
    <% } %>
  </div>
</body>
</html>
