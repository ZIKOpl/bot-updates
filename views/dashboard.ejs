<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Dashboard admin</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
  </head>
  <body style="font-family: system-ui, sans-serif; max-width: 900px; margin: 40px auto;">
    <h1>👑 Dashboard</h1>
    <p>Connecté en tant que <b><%= user.username %>#<%= user.discriminator %></b></p>
    <p>Version actuelle : <b><%= version %></b></p>

    <h2>Uploader une mise à jour</h2>
    <form action="/upload" method="POST" enctype="multipart/form-data">
      <p>
        <label>Version (ex: v2) :</label><br />
        <input type="text" name="version" placeholder="v2" required />
      </p>
      <p>
        <label>Fichier .zip :</label><br />
        <input type="file" name="updateZip" accept=".zip" required />
      </p>
      <button type="submit">Uploader</button>
    </form>

    <h2>Zips disponibles</h2>
    <% if (files.length === 0) { %>
      <p>Aucun fichier pour l’instant.</p>
    <% } else { %>
      <ul>
        <% files.forEach(f => { %>
          <li>
            <code><%= f.name %></code> (version <b><%= f.version %></b>)
            — <a href="<%= f.path %>">Télécharger</a>
          </li>
        <% }) %>
      </ul>
    <% } %>

    <p><a href="/">← Accueil</a> • <a href="/logout">Se déconnecter</a></p>
  </body>
</html>
