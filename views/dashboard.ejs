  <div class="box">
  <h2>📦 Gestion des mises à jour</h2>
  <p>Version actuelle : <strong><%= version %></strong></p>

  <form action="/upload" method="POST" enctype="multipart/form-data">
    <input type="file" name="file" accept=".zip" required />
    <button type="submit">📤 Envoyer le .zip</button>
  </form>

  <h3>Fichiers disponibles :</h3>
  <ul>
    <% files.forEach(file => { %>
      <li><a href="/releases/<%= file %>"><%= file %></a></li>
    <% }) %>
  </ul>

  <br>
  <a href="/logout"><button>Se déconnecter</button></a>
</div>
