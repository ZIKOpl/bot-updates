<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Dashboard | Bot Updates</title>
  <link rel="stylesheet" href="/style.css?v=3">
</head>
<body>
  <nav class="nav">
    <div class="brand"><div class="logo"></div><span>Dashboard</span></div>
    <div class="actions">
      <a href="/" class="btn ghost">Accueil</a>
      <a href="/logout" class="btn danger">Déconnexion</a>
    </div>
  </nav>

  <section class="hero fade-in">
    <h1>🧩 Panneau de gestion</h1>
    <p>Gère les versions et les mises à jour de ton bot en toute simplicité.</p>
  </section>

  <div class="container dashboard-flex">
    <!-- Upload Section -->
    <div class="upload-card glow-card">
      <h2>📤 Nouvelle Release</h2>
      <form class="upload-form" action="/upload" method="post" enctype="multipart/form-data">
        <input type="text" name="version" placeholder="Numéro de version (ex : 2.1)" required>
        <div class="drop-zone" id="dropZone">
          <p id="uploadText">Glisse ton fichier .zip ici ou clique pour le choisir</p>
          <input type="file" name="zip" accept=".zip" hidden required>
        </div>
        <button type="submit" class="btn primary">Uploader</button>
      </form>
    </div>

    <!-- Timeline Section -->
    <div class="timeline-card glow-card">
      <h2>🕓 Historique des versions</h2>
      <% if (releases.length === 0) { %>
        <p class="empty">Aucune release disponible</p>
      <% } else { %>
        <ul class="timeline">
          <% releases.forEach(r => { %>
            <li>
              <div class="dot"></div>
              <div class="timeline-content">
                <span class="version-label"><%= r.version %></span>
                <small><%= new Date(r.createdAt).toLocaleString("fr-FR") %></small>
              </div>
            </li>
          <% }) %>
        </ul>
      <% } %>
    </div>
  </div>

  <footer class="footer">© 2025 — Bot Updates Panel</footer>

  <script>
    const dropZone = document.getElementById("dropZone");
    const fileInput = dropZone.querySelector("input[type=file]");
    const uploadText = document.getElementById("uploadText");

    dropZone.addEventListener("click", () => fileInput.click());
    dropZone.addEventListener("dragover", e => {
      e.preventDefault(); dropZone.classList.add("hover");
    });
    dropZone.addEventListener("dragleave", () => dropZone.classList.remove("hover"));
    dropZone.addEventListener("drop", e => {
      e.preventDefault(); dropZone.classList.remove("hover");
      const file = e.dataTransfer.files[0];
      fileInput.files = e.dataTransfer.files;
      uploadText.textContent = "Fichier sélectionné : " + file.name;
    });
    fileInput.addEventListener("change", e => {
      const file = e.target.files[0];
      if (file) uploadText.textContent = "Fichier sélectionné : " + file.name;
    });
  </script>
</body>
</html>
