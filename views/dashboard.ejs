<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>Dashboard Admin</title>
  <style>
    body { background: #0e0e10; color: #fff; font-family: 'Segoe UI'; padding: 40px; }
    h1 { color: #5865f2; }
    form { margin: 20px 0; background: #18181b; padding: 20px; border-radius: 10px; }
    input, button { margin: 5px; padding: 10px; border: none; border-radius: 5px; }
    button { background: #5865f2; color: white; cursor: pointer; }
    button:hover { background: #4752c4; }
    .files, .stats { margin-top: 30px; background: #18181b; padding: 20px; border-radius: 10px; }
    ul { list-style: none; padding: 0; }
    li { margin: 5px 0; }
  </style>
</head>
<body>
  <h1>📂 Dashboard Admin</h1>
  <p>Connecté en tant que <%= user.username %></p>

  <form action="/upload" method="POST" enctype="multipart/form-data">
    <input type="text" name="version" placeholder="Nouvelle version (ex: v2)" required />
    <input type="file" name="updateZip" accept=".zip" required />
    <button type="submit">📤 Envoyer la mise à jour</button>
  </form>

  <div class="stats">
    <h2>📊 Statistiques</h2>
    <p>Téléchargements : <%= stats.downloads %></p>
    <p>Total bots : <%= Object.keys(stats.bots).length %></p>
  </div>

  <div class="files">
    <h2>📦 Fichiers disponibles</h2>
    <ul>
      <% files.forEach(f => { %>
        <li><%= f %></li>
      <% }) %>
    </ul>
  </div>
</body>
</html>
