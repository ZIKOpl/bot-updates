<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <title>HOME UPDATE • Dashboard</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <!-- NAV -->
  <nav class="nav">
    <div class="brand">
      <img src="/logo.png" alt="HOME UPDATE" class="nav-logo" />
      <span class="brand-text">HOME UPDATE</span>
    </div>
    <div class="nav-right">
      <a class="btn" href="/">Accueil</a>
      <a class="btn danger" href="/logout">Déconnexion</a>
    </div>
  </nav>

  <main class="container">
    <header class="hero">
      <h1>Dashboard des releases</h1>
      <p>Dépose une nouvelle version, ajoute des notes, et notifie automatiquement via Discord.</p>
    </header>

    <section class="dashboard-flex">
      <!-- Upload card -->
      <div class="card upload-card">
        <h2>📥 Nouvelle release</h2>
        <form id="uploadForm" class="upload-form" action="/upload" method="post" enctype="multipart/form-data">
          <input class="input" type="text" name="version" placeholder="Numéro de version (ex : 2.6 ou v2.6)" required />
          <textarea class="input textarea" name="notes" rows="3" placeholder="Notes de version (changements, corrections…)"></textarea>

          <div id="dropZone" class="drop-zone">
            <span id="uploadText">Glisse ton fichier .zip ici ou clique pour le choisir</span>
            <input id="fileInput" type="file" name="zip" accept=".zip" hidden />
          </div>

          <button type="submit" class="btn primary">Uploader</button>
        </form>
      </div>

      <!-- Timeline -->
      <div class="card timeline-card">
        <h2>🕒 Versions précédentes</h2>
        <% if (releases.length === 0) { %>
          <p class="muted">Aucune release disponible</p>
        <% } else { %>
          <ul class="timeline">
            <% releases.forEach(r => { %>
              <li>
                <span class="dot"></span>
                <div class="timeline-content">
                  <div class="version-label"><%= r.version %></div>
                  <div class="muted"><%= new Date(r.createdAt).toLocaleString("fr-FR", { dateStyle:"medium", timeStyle:"short" }) %></div>
                  <% if (r.notes) { %><p class="muted" style="margin-top:6px"><%= r.notes %></p><% } %>
                </div>
              </li>
            <% }) %>
          </ul>
        <% } %>
      </div>
    </section>
  </main>

  <footer class="footer">© <%= new Date().getFullYear() %> — Home Update Panel</footer>

  <script>
    // Drag & Drop + click
    const drop = document.getElementById('dropZone');
    const input = document.getElementById('fileInput');
    const text = document.getElementById('uploadText');
    const form = document.getElementById('uploadForm');

    drop.addEventListener('click', () => input.click());
    drop.addEventListener('dragover', (e) => { e.preventDefault(); drop.classList.add('hover'); });
    drop.addEventListener('dragleave', () => drop.classList.remove('hover'));
    drop.addEventListener('drop', (e) => {
      e.preventDefault();
      drop.classList.remove('hover');
      if (e.dataTransfer.files.length) {
        input.files = e.dataTransfer.files;
        text.textContent = 'Fichier sélectionné : ' + e.dataTransfer.files[0].name;
      }
    });
    input.addEventListener('change', () => {
      if (input.files.length) text.textContent = 'Fichier sélectionné : ' + input.files[0].name;
    });

    form.addEventListener('submit', () => {
      text.textContent = 'Upload en cours…';
    });
  </script>
</body>
</html>
